# 2026-09-14

## Screeps 4.3 引擎集成测试环境

- 引入并精确锁定 `screeps-integration-tests@3.0.0`，通过其预构建 mockup fork 在 Node.js 24 下
  启动官方 Screeps 4.3 storage、engine runner 与 processor；lockfile 固定 driver、engine、
  common、storage 和 `isolated-vm` 的实际解析版本。
- 为 npm 12 配置官方 driver 所需的 Git 依赖权限，并用 `allowScripts` 白名单固定五个 Screeps
  原生/构建脚本。node-gyp 使用 uv 在仓库内创建且已忽略的 `.venv`，虚拟环境不进入提交。
- 新增 `screeps-integration.config.cjs`、`npm run test:integration` 与 `npm run test:all`；场景串行运行，
  数据与性能输出进入已忽略目录，不读取 `.secret.json`，也不执行上传。
- 新增 `leviathan-runtime` 场景：正式 Rollup bundle 在真实玩家 isolate 连续执行三 tick，验证生产
  loop、可见房间、CPU API、游戏内 sourcemap 模块与空 Memory 语义。测试探针只修改内存中的
  module wrapper，不改写源码或部署产物。
- 增加 [集成测试环境说明](../testing/integration.md) 和测试索引，并同步项目首页、文档总导航与
  changelog 索引；记录安装前提、权限边界、场景规范、清理机制和暂未覆盖的 global reset、
  Segment 及完整私服 backend。
- 验证：Node.js 24.20.0 与 uv Python 3.13.5 环境下，真实引擎场景 3 tick / 3 次生产 loop 通过，
  console 无运行错误，Memory 保持 `{}`；完整类型检查、快速测试、构建与差异检查一并通过。
