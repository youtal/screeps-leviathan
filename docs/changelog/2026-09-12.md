# 2026-09-12

## 文档结构与规范

- 设计与使用说明按源码模块归属迁入 `core/`、`modules/`；Core 架构放在设计目录的 `core/README.md`，MemoryManager 放在 `core/memoryManager.md`。
- 建立 [文档总导航](../README.md)，同步项目首页和分类索引，明确缺失使用说明及规划模块的交付状态。
- AGENTS.md 规定设计案只记录设计意图和交付状态，禁止项目现状对照；补充模块路径对应、导航维护及引用更新要求。
- 清理设计文档中的现状盘点和差异叙述，保留设计意图、待决事项与交付标记。Goto 接口差异的历史记录保留在 [2026-09-11](./2026-09-11.md)。
- 同步文档及源码注释引用路径；此文档调整阶段不变更运行时代码行为。

## 公共契约与持久化拆除

- 新建 src/contracts，按日志、环境、总线、事件载荷、Profiler、错误映射、意图、插件、Runtime 和 Memory 分拆公共类型；实现与消费者改为引用独立契约。
- createLog、createBus、createProfiler、createErrorMapper、createFramework 显式承诺返回接口。LogOptions 不再是环境全局类型；监听器仓库、Profiler 统计 schema 和 Framework 健康表仍在所属模块。
- 移除 Framework 的 memoryInterceptor.ts、memoryFragments.ts 和旧 persistence/migrate 接入、MemoryPort、检查点配置。注册表保留独立 ID 校验，并明确拒绝旧插件持久化声明。
- 经确认，Framework 不再解析、挂载或写回 Memory/RawMemory；插件状态不再由框架持久化，健康记录和默认 Profiler 统计只在实例 heap 内保留，global reset 后清空。未删除、迁移或覆盖已有游戏存储。
- core/memoryManager 与 core/logger 仅建立 .gitkeep 占位；未实现新存储或移动现有日志工厂。MemoryManager 发布申请/访问契约，pending 只影响依赖 Memory 的行为，不暂停整个模块。
- MemoryManager 设计迁入 core/memoryManager.md，同步导航、使用状态和公共行为说明；Goto 保留内部数据 schema，移除对旧泛型 PluginContext 持久化入口的类型绑定。
- 移除旧实现专属测试，保留调度与错误隔离覆盖；补充存储零访问、reset 后健康状态清空、旧声明拒绝和契约编译期负例，更新真实 bundle 回归。

## 验证

- `npx tsc --noEmit` 通过，包含契约编译期负例。
- `npm test` 通过：8 组、73 个 Jest 用例，4 个构建插件用例和 2 个真实 bundle 用例。上传测试使用模拟请求，不连接游戏环境。
- 清除部署目标环境变量后执行 `npm run build` 成功，仅构建、不上传；默认构建路径不读取 `.secret.json`。
- 文档相对链接检查与 `git diff --check` 通过。

## Logger 落地

- 新增 `src/core/logger`：`createLogging` 按装配配置返回 `LoggerFactory`，`scope(name, options)` 派生带前缀的日志器；等级「作用域覆盖 → 装配覆盖 → setting 默认」逐字段用 `??` 回退，关闭等级在格式化前短路。
- `src/contracts/logging.ts` 增补 `LogOutput`、`LoggingOptions`、`ScopeLogOptions`、`LoggerFactory`，把输出端口与作用域派生协议纳入公共契约；`test/contracts.types.ts` 增加日志契约正例与等级键负例。
- 双通道输出：默认 `console.log` 与 `Game.notify(line, notifyInterval)`（分组间隔默认 60 分钟，正整数校验在装配期完成）；邮件策略为装配级 `off | error`，作用域可覆盖，只有 error 等级会发送。端口抛错只丢弃当条日志，日志不依赖 Memory/Profiler/ErrorMapper/Game。
- 日志实现从 `utils/console` 迁出：该目录只保留着色与模板等纯函数（Logger 单向复用 `Color/dyeText`），`src/utils/index.ts`、`console/index.ts`、`setting.ts` 注释同步；`core/*` 不再从 utils 导入日志实现。
- 装配接线：`RuntimeOptions.logging` 与 `FrameworkOptions.logging` 支持注入；`createRuntime` 装配共享工厂并注入模块 env、Profiler 与 EventBus，`createFramework` 同样注入 ErrorMapper、EventBus 与插件 env，未注入时使用 `defaultLoggerFactory` 兜底，保证独立调用与测试可用。
- `createEnvMethods` 的 `notify` 参数改为可选覆盖：`undefined` 跟随装配策略、`false` 强制关闭、`true` 在装配允许时开启；默认行为与之前一致（项目默认不发邮件）。
- 测试：新增 `test/logger.test.ts` 9 个用例（默认等级、作用域覆盖、着色前缀、邮件策略与覆盖、端口抛错隔离、非法间隔、无 Game 环境、默认端口写入与 notify 间隔），`test/console.test.ts` 移除已迁走的等级用例并保留模板回归；`test/contracts.types.ts` 扩充编译期负例。
- 文档：`docs/design/core/logger.md` 与 `docs/usage/core/logger.md` 由占位改为正式设计与使用说明；Runtime、Framework 的设计与使用说明补充日志装配；契约文档、总导航与两级索引同步 Logger 交付状态。
- 验证：`npx tsc --noEmit` 通过；`npm test` 通过（9 组、80 个 Jest 用例、4 个构建插件用例、2 个真实 bundle 用例）；`npm run build` 成功（不依赖 `.secret.json`）；`git diff --check` 与文档相对链接检查通过。

## Logger 评审修复

- 邮件策略改为**硬上限**：装配级 `notify: 'off'` 时作用域不能用 `notify: true` 打开邮件；装配为 `'error'` 时作用域只能跟随（`undefined`）或关闭（`false`）。契约注释、设计与使用文档同步；新增「装配 off + 作用域 true」用例，该用例在修复前的实现上判失败。
- ErrorMapper 默认报告出口改为在创建映射器时派生一次 `ErrorMapper` 作用域日志器，故障密集时不再逐次创建日志器对象与临时字符串；新增回归用例断言 `scope` 只调用一次、每次故障仍写入。
- 作用域着色前缀改为按等级惰性缓存：每个作用域每个等级只调用一次 `dyeText`，之后每条日志只做一次字符串拼接。
- 内容约定澄清：设计文档不再承诺"日志文本单行"。Logger 不改写内容，普通业务日志约定单行，错误堆栈允许多行并原样保留。
- 「单一组合根」记入 Logger 设计待决事项与 Core 架构 §5：正式 App 只允许一个内核装配入口，Runtime 以完整内核运行时对象交付能力（日志、事件总线、观测、错误映射、存储端口），Framework 消费而不重建；各能力兜底实例仅服务独立测试。该形态随统一内核装配一并交付。
- 契约收尾：`LogOutput` 注释删除“单行”表述，并说明端口文本可能包含换行（错误堆栈），实现不得假设单行。
- 清理：`src/core/logger/.gitkeep` 占位文件随模块落地移除，目录已有真实实现。
- 验证：`npx tsc --noEmit` 通过；`npm test` 通过（9 组、82 个 Jest 用例、4 个构建插件用例、2 个真实 bundle 用例）；`npm run build` 成功；`git diff --check`、改动 TypeScript 文件的 prettier 检查与文档相对链接检查通过。

## MemoryManager 落地

- 新增 `src/core/memoryManager`：`createMemoryManager` 实现主 Memory 命名空间（schemaVersion/allocations/rawPartitions/migration）、Raw 与 Segment 双后端、启动申请窗口与固定 10 页分配、数据版本迁移、journal 驱动的可恢复搬迁，以及 `getStatus()` 诊断快照；平台端口可注入，默认直连 RawMemory/Segment。
- 契约补充 `MemoryHost`（begin/end/deferStartupWindow/bind），`PluginContext.memory` 与 `ModuleContext.memory?` 提供按 owner 绑定的申请入口；Framework 在 tick 边界驱动 Memory 生命周期，并在安全模式或插件因 CPU 未准入时延后封存窗口，Runtime 支持 `memory` 选项并为模块名绑定入口。
- 提交语义：critical 当 tick、checkpoint 按首次 dirty 起算；Raw 分区只重算变化片段并在整串写入成功后清 dirty，Segment 分区只写自己的信封且单页失败隔离；写入失败保留 dirty 与诊断。
- 数据安全：未知 schema、页归属不符、版本降级、缺少 migrate 的升级一律拒绝写入；旧 `leviathan` 命名空间一次性导入插件 payload 且不删除不改写；非托管根字段按片段保留并与宿主 Memory 的替换/新增字段合并。
- 测试：新增 `test/memoryManager.test.ts` 13 例（申请与冲突、提交与 clean tick、checkpoint 到期、优先级分配与落选、跨 global 恢复、迁移中断恢复、窗口延后、未知 schema、旧布局导入、根字段合并、写失败保留 dirty、版本迁移与降级、pending 隔离）与 framework 集成 2 例（按 pluginId 绑定并跨 reset 恢复、未装配时抛配置错误）。
- 文档：MemoryManager 设计与使用说明由占位更新为正式文档（含首版实现约定与未交付清单），契约、Runtime/Framework 文档、总导航与两级索引同步；移除 `src/core/memoryManager/.gitkeep` 占位。
- 验证：`npx tsc --noEmit` 通过；`npm test` 通过（10 组、97 个 Jest 用例、4 个构建插件用例、2 个真实 bundle 用例）；`npm run build` 成功；`git diff --check`、改动 TypeScript 文件的 prettier 检查与文档相对链接检查通过。本模块尚未提交，等待确认。

## MemoryManager 评审修复（未提交）

- **页所有权（阻断）**：新增页内容观察与保留机制——只有"读到过且为空"的页参与分配，目录与迁移 journal 引用的页视为自有，其他工具数据与无归属信封登记为保留页并在 `getStatus().reservedSegments` 暴露；迁移 copy 阶段拒绝写入非空目标页；激活请求改为提交精确页集合（不再并入外部活动页，避免超过单 tick 10 页上限）；未被本 global 申请模块持有的页不再被视为空闲。
- **ready 句柄时效（阻断）**：`access()` 返回的 ready 句柄绑定签发 tick 与数据引用，跨 tick、分区 pending 或数据重载后调用 `query()/commit()` 抛协议错误；契约与使用说明同步该语义。
- **Framework 异常隔离（阻断）**：Memory `begin/end` 移入错误边界并在 `finally` 内层 try/finally 中收尾，任何存储异常都记录为内核故障且一定复位 `running`，不再出现永久"loop is not reentrant"。
- **安静 tick（阻断）**：外部根字段改为写入时现取现序列化 + 键集合/引用变化检测，默认环境下 clean tick 不再每 tick 调用 `RawMemory.set`，片段缓存收益恢复。
- **迁移代际**：新增持久单调 `generationCounter`，每次搬迁取新值，旧页残留信封不会被回读校验误认成本次写入。
- **无模块恢复**：迁移数据改为从存储与已冻结副本搬运，模块本轮未申请也能完成 journal 恢复；中止时清理目标页并保留诊断。
- **窗口完整性**：插件 setup 失败也计入申请可能未收齐；窗口最多延后 `maxStartupDeferrals`（默认 10）个 tick，超限强制封存并在状态中标记。
- **命名空间校验加强**：allocations/rawPartitions/migration 逐项深度校验，半损坏数据直接进入故障状态而不是遍历时崩溃；旧布局导入改为深拷贝，避免模块原地修改污染旧命名空间。
- 测试：新增 11 例回归（外来页保留、未认领信封保留、未申请模块的页保留、ready 句柄跨 tick 失效、带宿主 Memory 的 clean tick 不写、抢占后页复用与代际单调、模块缺席的 journal 恢复、窗口强制封存、精确激活集合、end/begin 抛错不卡死、setup 失败延后窗口），`npm test` 达到 10 组 / 110 项 Jest 用例 + 4 项构建插件 + 2 项 bundle。
- 文档：MemoryManager 设计与使用说明更新页所有权、句柄时效、窗口上限、存储驱动恢复与应用层装配状态；契约与 Framework 设计交付状态同步。

## MemoryManager 接入 Logger 与通用规范固化

- MemoryManager 新增 `logging?: LoggerFactory` 选项（缺省内核兜底工厂），作用域固定 `MemoryManager` 且每实例派生一次；落点：加载失败与不可自愈数据问题 `error`、写入失败/页被占用/容量超限/窗口强制封存 `warn`（同一原因只记一次）、迁移阶段切换与恢复完成 `info`、pending 往返 `debug`；提交热路径不输出，日志端口抛错不影响存储流程。
- 新增 5 例日志回归：加载失败跨 tick 只记一次、默认等级下正常路径完全静默且 info 能看到迁移四阶段、保留页告警只出现一次、写失败同一消息只告警一次且重试行为不变、输出端口抛错时管理器仍正常工作。
- 通用规范固化：[Core 架构 §10](../design/core/README.md) 新增「内核能力使用 Logger 的通用规范」（注入而不自建、作用域固定且每实例一次、只在状态迁移与故障输出、一次事件一次、日志不是唯一诊断、邮件策略跟随装配、测试要求）；Logger 设计与使用说明、MemoryManager 设计与使用说明同步引用该规范。
- 验证：`npx tsc --noEmit` 通过；`npm test` 通过（10 组、115 项 Jest 用例、4 个构建插件用例、2 个真实 bundle 用例）；`npm run build` 成功；`git diff --check`、改动 TypeScript 文件的 prettier 检查与文档相对链接检查通过。本轮改动与 MemoryManager 实现一并留在工作区，尚未提交。

## 应用层装配与 Memory 访问边界

- `src/app/runtime.ts` 成为唯一组合根：按「日志工厂 → MemoryManager → Framework」的顺序装配一次，并把同一个实例注入 Framework；模块名到分区入口的绑定在 Runtime 完成，业务模块只通过 `context.memory` 申请。
- AGENTS.md 新增第 9 节「Memory 访问边界」：`core/memoryManager` 是项目内唯一允许访问全局 `Memory`、`RawMemory` 与 Segment 的模块，其它模块必须走 `context.memory`，直接把存储访问写进别的模块按阻断问题处理。
- 新增 `test/memoryBoundary.test.ts`：扫描 `src/`（排除 memoryManager 自身）拦截 `RawMemory`、`globalThis.Memory`、`Memory.`/`Memory[`/`Memory =` 等直接存储访问，并用自检用例保证规则本身不会静默失效。
- 文档同步：`docs/usage/core/memoryManager.md` 的「应用层装配」标注唯一组合根与访问边界，`docs/design/core/README.md` §7 增补边界条目，MemoryManager 设计文档移除「应用层尚未装配」的待交付项。
- 验证：`npx tsc --noEmit` 通过；`npm test` 通过（11 组、117 项 Jest 用例、4 个构建插件用例、2 个真实 bundle 用例）；`npm run build` 成功；`git diff --check`、改动 TypeScript 文件的 prettier 检查与文档相对链接检查通过。

## MemoryManager 审计问题关闭（AUDIT.md 已按约定移除）

审计报告 `src/core/memoryManager/AUDIT.md`（临时文档）的 3 条 P1/P2 数据安全问题、5 条 P3 加固项与用例缺口逐条关闭；报告本身按第 7 节约定在问题关闭后删除，结论并入本条记录。

- **P1-1 脏数据被旧信封覆盖**：`restorePartition` 不再覆盖已装载数据，`begin` 重试循环对 dirty 分区只清 pending、保留 heap 修改；页短暂不可见只推迟提交。
- **P1-2 switch 后"无 pending 且无数据"**：`switch` 对未装载的 resident 显式回到 `loading`，重试条件改为"缺数据也重试"，`getStatus().pending` 按 accessor 实际判定推导；不再出现永久 loading 与状态失真。
- **P2-1 版本 0 被当作新安装**：只有完全没有历史记录才算首次安装；任何已存储数据（含旧布局导入的版本 0 = 未知旧版本）都必须经 `migrate`，缺少 migrate 一律拒绝写入。使用说明同步该规则。
- **P2-2 腾退页清理**：新增 `clearVacatedSegment`（可见性判断 + 错误边界 + 诊断），清理失败不阻断目录切换；`abortMigration` 复用同一实现。
- **P2-3 未装载分区参与搬迁**：分配规划排除 `data === null` 或 `recovery` 的候选，并在 `getStatus().allocationSkipped` 中给出落选原因。
- **P3**：宿主删除根字段不再被解析快照"复活"；命名空间校验拒绝 `__proto__`/`prototype`/`constructor` 并改用 null 原型容器；`initialize`/`migrate` 返回值做键值对象运行时校验；删除 `firstTick` 死代码；目录/journal 引用未托管页时给出 `not managed` 诊断而不是无限 `segment-activating`。
- **用例缺口**：补充 priority 0 参与排名、同分按稳定身份决胜、Segment 分区提交不重写主 Memory、迁移期容量超限中止并保留 Raw 数据，以及上述 P1-1/P1-2/P2-1/P2-2/P2-3/P3 全部回归；P1-1、P1-2、P2-1 三条用例已逐一验证在修复前判失败。
- 验证：`npx tsc --noEmit` 通过；`npm test` 通过（10 组、127 项 Jest 用例、4 个构建插件用例、2 个真实 bundle 用例）；`npm run build` 成功；`git diff --check`、改动 TypeScript 文件的 prettier 检查与文档相对链接检查通过。改动仍留在工作区，尚未提交。

## MemoryManager 第 2 轮审计关闭（AUDIT.md 再次按约定移除）

第二轮审计确认第 1 轮全部问题关闭，并提出 1 条新回归、1 条加固遗漏与 3 项语义/注释问题；逐条处理如下，报告在关闭后删除，结论并入本条。

- **P1-F（新回归）**：`begin` 重试循环的 dirty 快捷分支会一并清掉迁移冻结，导致搬迁窗口内的提交被接受、随后在 switch 清 dirty 时静默丢失。修复为"冻结类 pending（migration/verification）必须保持"，并补回归用例（checkpoint 分区在 copy/verify/switch 全程 `access()` 必须是 pending，switch 后页内容等于冻结时刻数据）；该用例已验证在修复前判失败。
- **清理阶段（本轮复核 §5 失败注入时发现的数据安全缺口）**：原实现 switch 当 tick 清空被腾退页，若同 tick 主 Memory 写入失败并发生 global reset，会留下"存储目录仍指向已清空页"的悬空引用。迁移阶段新增 `cleanup`：目录切换后先随整串写入落盘，成功后的下一 tick 才清页与暂存；写失败停在 cleanup 重试，页不可见时重试至多 3 次后放弃并保留为保留页。新增"switch 期写入失败仍保留有效副本"回归（已验证修复前判失败）。
- **P3-A**：旧布局导入路径未做原型键加固——`importLegacy` 现在跳过 `__proto__`/`prototype`/`constructor` 并记录诊断，`createEmptyNamespace` 改用 null 原型容器；新增回归用例。
- **P3-B**：`recovery` 无数据时会重读自愈，原注释与之不符；保留自愈并把注释、设计与使用说明改为描述真实行为。
- **P3-C**：`writeError` 在不同恢复路径上的清理不一致；统一语义为"最近一次故障，可能已恢复"，当前可用性以 `pending` 与 `access()` 为准，字段说明与使用说明同步。
- **P3-D**：`Partition.data` 注释过时（迁移冻结不置空数据），已与实现同步。
- **用例缺口**：补齐超过 10 个申请（12 申请 / 10 页，后两名落选留 Raw）与迁移期写入失败的"至少一个有效副本"断言；设计 §10 清单里其余的逐步故障注入细节（verify 阶段读取异常、暂存容量专项计量）登记为后续独立任务，不与本轮关闭混淆。
- 验证：`npx tsc --noEmit` 通过；`npm test` 通过（10 组、131 项 Jest 用例、4 个构建插件用例、2 个真实 bundle 用例）；`npm run build` 成功；`git diff --check`、改动 TypeScript 文件的 prettier 检查与文档相对链接检查通过。改动仍留在工作区，尚未提交。

## MemoryManager 第 3 轮审计关闭（AUDIT.md 按约定移除）

第三轮复审计**未发现 P1/P2 问题**，确认第 1、2 轮全部发现关闭，并独立复核了新增的 `cleanup` 阶段与 `migrationPersisted` 门控（写入失败窗口、清理重试上限、腾退与迁入的顺序安全、重启后的结构校验）——结论为逻辑自洽。本轮只处理其提出的引用清理与两项观察：

- **P3-E-1**：源码注释中"（第 2 轮审计 §5 的失败注入路径）"指向已删除的文档与不存在的章节，改为直接描述失败场景（写失败叠加 global reset 会产生悬空引用）。
- **P3-E-2**：`test/memoryManager.test.ts` 头部对 `AUDIT.md` 路径的引用改为指向长期记录 `docs/changelog/2026-09-12.md`，并直接列出覆盖的不变量。
- **观察项 1**：cleanup 失败告警按代际去重——同一 generation 只在首次尝试告警一次，放弃时再记一条，贴合 Core 架构"同一原因只记一次"；回归用例补断言（首次告警 1 条 + 放弃 1 条，重试静默）。
- **观察项 2**：设计 §10 的验证要求保留两条剩余验证项（verify 阶段读取异常注入、主 Memory 暂存容量专项计量），不再只登记在变更记录里。
- 验证：`npx tsc --noEmit` 通过；`npm test` 通过（10 组、131 项 Jest 用例、4 个构建插件用例、2 个真实 bundle 用例）；`npm run build` 成功；`git diff --check`、改动 TypeScript 文件的 prettier 检查与文档相对链接检查通过。报告已删除，改动仍留在工作区未提交。

## 提交切分

- 本轮工作区改动按两个提交落地：`feat(memoryManager)` 交付 core/memoryManager 内核存储模块、契约与统一内核装配（含应用层装配与 Memory 访问边界规范），`fix(memoryManager)` 关闭上文三轮审计的全部发现。
- 变更记录里的验证计数按各自提交时的树记录：feat 为 11 组 / 117 项 Jest 用例，fix 关闭后为 11 组 / 133 项 Jest 用例（另有 4 项构建插件与 2 项真实 bundle 用例）；console 目录清理单独成一次提交，不并入上述两条。
