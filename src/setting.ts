/**
 * 默认log显示配置
 */
export const DEFAULT_LOG_CONFIG = {
  debug: false,
  warning: true,
  error: true,
  success: false,
  info: false,
  report: true,
};

/**
 * 默认不启用Profiler
 */

export const DEFAULT_PROFILER_ENABLE = false;
