# Screeps Leviathan 开发规范

本文件适用于整个仓库。子目录的 `AGENTS.md` 可以补充本规范；没有用户明确授权，不得放宽其中的约束。项目开发产生的说明性文档，除根目录的 `README.md` 和本文件外，统一放在 `docs/`。

## 1. 项目与查找入口

本项目使用 TypeScript 实现长期运行的 Screeps 游戏 AI。设计和实现需要考虑每 tick CPU 消耗、global heap 占用、Memory 序列化成本、跨 tick 状态一致性，以及 global reset 后的恢复。

- `src/contracts/`：跨模块协议；`src/core/runtime/`：Core 的唯一组合根；`src/core/framework/`：消费完整 Runtime 的框架；`src/app/`：应用装配。
- `src/modules/`：游戏业务模块；`src/utils/`：通用工具；`test/`：单元测试和架构边界检查。
- `docs/README.md`：源码、设计和使用说明的总导航；`docs/design/`：设计意图与交付状态；`docs/usage/`：已交付公共能力的使用方法；`docs/testing/`：测试环境与场景。

开始任务时先读相关源码、契约、设计和使用说明；目录结构或命令不确定时，以仓库中的文件和 `package.json` 为准，不凭记忆猜测。

## 2. 任务范围与交付

以用户明确提出或明确认可的范围为边界。未经用户明确同意，不扩展功能、不重构无关模块、不改变无关业务行为、不引入额外依赖。范围外问题应说明位置和影响并提出建议；只有它直接阻断当前任务时，才在当前任务中处理。

修改前确认受影响的公共契约、文档和测试；修改后检查差异，报告变更、验证结果和未解决的限制。不要把未运行的检查写成已通过，也不要为满足流程制造无意义的测试或文档。

## 3. 源码注释

`src/` 下所有源文件都应有清晰、详细且与实现同步的注释，使后续开发者能理解设计理由、运行机制和维护边界。

每个源文件在 import 之前写文件摘要；若语言工具要求指令位于首行，摘要紧随该指令。摘要至少说明文件解决的问题及职责、所属模块及位置、主要输入输出或对外能力，以及重要状态、缓存、外部依赖或运行时副作用。

依据实际复杂度，注释还应解释以下适用事项：

- 方案选择、算法原理、执行顺序、复杂度及性能取舍；
- 关键实现细节、边界条件、失败路径、降级行为和维护约束；
- 非直观的 TypeScript 类型技巧、类型收窄或 JavaScript/TypeScript 语法；
- 关键变量、状态容器、缓存、计数器及版本字段的作用和生命周期；
- 第三方包的用途、调用约定，以及所依赖的具体行为；
- Screeps API 的运行时限制、CPU 或 Memory 成本及跨 tick 影响；
- 其他有助于理解、测试、排错和后续修改的非直观信息。

注释解释“为什么”和“如何工作”，不逐句复述代码。新增或修改源文件时，使涉及的文件满足本节；任务范围外的既有注释缺口先报告，等待用户决定是否批量补充。

## 4. 实现与状态

默认采用不严格的函数式编程：优先使用小型函数、明确的输入输出、组合、依赖注入和可预测的数据流，限制共享可变状态及隐式副作用。

有明确的 Screeps 性能收益时，可以在模块内部受控地原地更新，或用闭包保存缓存、索引、状态和预计算结果。此时须在注释中交代状态的创建位置、命中与更新方式、失效与清理条件、跨 tick 有效性、global reset 后行为，以及性能收益和一致性风险。

## 5. 持久化 Memory 边界

`src/core/memoryManager` 是游戏持久化存储的唯一入口。除该模块及其平台适配层外，`src/` 中的文件不得直接读取、修改或绕过持久化存储，包括：

- 全局 `Memory` 的读写或删除，以及用 `globalThis.Memory` 挂载替身；
- `RawMemory.get`、`RawMemory.set`、`RawMemory.segments` 和 Segment 激活接口；
- 自行解析存储文本，或将跨 global 状态塞入外部共享对象等旁路。

需要跨 global 保留状态的模块通过 `context.memory(localId, options)` 申请分区。分区处于 `pending` 时，只跳过依赖存储的行为，其余逻辑照常执行。越界访问是阻断问题；`test/memoryBoundary.test.ts` 会扫描 `src/`。

## 6. Core 依赖边界

`src/core/` 下的同级能力模块不得直接导入彼此的具体实现。模块间只依赖 `src/contracts/` 发布的协议，由调用方显式注入实例；不得通过默认参数、模块级单例或临时工厂在模块内部创建同级依赖的兜底实现。

`src/core/runtime/` 是唯一的 Core 组合根，可以导入 Logger、EventBus、MemoryManager、Profiler、ErrorMapper 和 TaskScheduler 的具体工厂，按依赖有向无环图从前序到后序创建实例，再将完整 Runtime 交给 Framework。前序模块不得依赖后序模块；Framework 只消费 Runtime 契约，不创建或替换其中的 Core 能力。

独立单元测试也须在测试装配边界显式提供依赖，不能为了测试便利恢复生产代码中的隐藏缺省依赖。`test/coreDependencyBoundary.test.ts` 会扫描 Core 源码中的跨模块导入。

## 7. 模块文档

已交付的模块按 `src/` 下的模块目录分别维护两类文档，目录名称及大小写一致，不逐个源码文件建立镜像：

- `docs/design/<模块相对路径>.md`：只写设计意图和交付状态。设计意图可包括定位、目标、架构、协议、算法、状态模型、性能、边界、风险和待决事项；交付状态标记未交付、部分交付、已交付及各设计项的状态。
- `docs/usage/<模块相对路径>.md`：面向使用者，记录初始化、公共 API、参数、返回值、配置、调用示例、错误处理和注意事项。

例如 `src/core/runtime/` 对应 `docs/design/core/runtime.md` 与 `docs/usage/core/runtime.md`。跨模块架构设计放在所属层级的 `README.md`，例如 `docs/design/core/README.md`。新增已交付模块时同时创建两类文档；仅规划、尚未交付的模块可以按目标源码路径创建设计文档并标注交付状态，不编造使用说明或链接到不存在的源码。修改模块公共行为、配置或返回契约时同步更新两类文档。

新增或修改设计案时，不写项目现状盘点、现状与目标对照、重构前后差异或既有代码与设计的差距分析，也不借交付状态展开这类对照。迁移协议、兼容约束和恢复机制可以作为设计意图描述。现有公共能力的用法写在使用说明，已完成变更的历史记录写在 `docs/changelog/`。

根目录 `README.md` 链接 `docs/README.md`；总导航按模块列出源码、设计和使用说明，并明确标注缺失项。`docs/design/README.md`、`docs/usage/README.md` 分别是两类索引。模块文档新增、移动或更名时，同步更新导航、索引及所有引用，包括源码注释中的路径。完成阶段性更新记录时，按日期写入 `docs/changelog/` 并更新其索引。

## 8. 验证与上传

完成代码或构建系统修改后，按顺序运行与变更相符的检查；通常为：

```bash
npx tsc --noEmit
npm test
npm run build
git diff --check
```

`npm test` 包含 Jest 和构建相关检查。真实 Screeps 引擎集成测试使用 `npm run test:integration`；需要该级别验证时先读 `docs/testing/integration.md`。纯文档修改至少运行 `git diff --check`，并检查改动文档的链接及引用。检查失败时报告命令、原因和影响。

`npm run build` 必须在没有 `.secret.json` 时也能成功。验证环境上传统一使用 `npm run upload:validation`；只有用户明确要求部署时才执行外部上传。

## 9. 密钥与敏感信息

不得提交 `.secret.json`、认证令牌、账号凭据或包含真实凭据的命令输出。公开配置示例只使用占位值；报告、测试夹具和文档也不得泄露真实凭据。

## 10. Git 提交身份

任何 agent 完成项目更改并执行 Git 提交时，提交标题、正文、附加署名和提交元数据中不得体现 agent 或 AI 身份，也不得添加相关标识、致谢或生成声明。使用仓库现有的 Git 身份配置，不伪造或改写用户身份；身份配置缺失时，先请用户提供。
