# Screeps Leviathan 开发规范

## 1. 文件作用域

本文件位于仓库根目录，其规范适用于整个项目。除根目录的 `README.md` 和本文件外，项目开发产生的说明性文档统一放在 `docs/` 目录下。

如果子目录中存在更具体的 `AGENTS.md`，子目录文件可以补充本规范，但不得在没有用户明确授权的情况下放宽本文件的约束。

## 2. 项目目标

本项目的最终开发目标是实现一个基于 TypeScript 的自动化编程游戏《Screeps》游戏 AI。

设计和实现应结合 Screeps 的长期运行环境，关注每 tick CPU 消耗、global heap 占用、Memory 序列化成本、跨 tick 状态一致性及 global reset 后的恢复能力。

## 3. 范围控制

开发过程中，用户会尽可能详细地描述需求。必须以用户明确提出或明确认可的范围为边界；未经用户明确同意，严禁随意扩展功能、重构无关模块、改变业务行为或引入额外依赖。

发现范围外问题时，应记录问题、说明影响并向用户提出建议。除非它会直接阻断当前任务，否则不得借当前任务顺带修改。

## 4. 源码注释规范

`src/` 下的所有源文件都应具有清晰、详细且与实现同步的注释。注释的目标是让后续开发者能够理解设计理由、运行机制和维护边界，而不必重新推导代码。

### 4.1 文件摘要

每个源文件顶部都应包含文件摘要，至少说明：

- 当前文件解决的问题和主要职责；
- 它所属的模块及在模块中的位置；
- 主要输入、输出或对外能力；
- 重要状态、缓存、外部依赖或运行时副作用。

文件摘要应位于 import 之前；如果语言工具要求特定指令必须处于首行，则摘要紧随该指令之后。

### 4.2 详细注释

根据代码实际复杂度，注释应覆盖以下内容：

- 代码意图以及采用当前方案的原因；
- 算法原理、执行顺序、复杂度和性能取舍；
- 关键实现细节、边界条件、失败路径和降级行为；
- TypeScript 类型技巧、泛型、条件类型、映射类型及类型收窄方式；
- JavaScript 或 TypeScript 语言特性和语法糖的实际含义；
- 关键变量、状态容器、缓存、计数器和版本字段的作用及生命周期；
- 第三方包的用途、调用约定，以及项目依赖其提供的具体行为；
- Screeps API 的运行时限制、CPU 或 Memory 成本及跨 tick 影响；
- 其他对理解、测试、排错和后续修改有帮助的信息。

注释应解释“为什么”和“如何工作”，并与代码保持一致。不得用大量逐字复述语句含义的注释掩盖不清晰的实现。

新增或修改源文件时，必须使涉及的文件满足本规范。对于任务范围外的既有注释缺口，应先报告并等待用户决定是否开展批量补充，避免借注释工作扩展当前任务。

## 5. 编程范式

本项目遵循不严格的函数式编程范式。

默认遵循函数式编程的基本原则：优先使用小型函数、明确输入输出、组合、依赖注入和可预测的数据流；尽量限制共享可变状态和隐式副作用。

在 Screeps 性能收益明确时，可以向性能妥协，包括利用 JavaScript 闭包保存缓存、索引、状态和预计算结果，也允许在模块内部进行受控的原地更新。此类状态必须封装在清晰的生命周期边界内，并通过注释说明：

- 缓存或可变状态的创建位置；
- 命中、更新、失效和清理条件；
- 跨 tick 是否有效；
- global reset 后的行为；
- 采用可变实现所换取的性能收益及一致性风险。

## 6. 模块文档

每个模块都应在 `docs/` 下拥有两类独立文档：

- `docs/design/<模块相对路径>.md`：只记录设计意图和交付状态。设计意图包括模块定位、设计目标、目标架构、协议、算法、状态模型、性能考虑、边界条件、风险和待决设计事项；交付状态包括未交付、部分交付、已交付及各设计项的交付标记；
- `docs/usage/<模块相对路径>.md`：面向模块使用者，记录初始化方式、公共 API、参数、返回值、配置项、调用示例、错误处理和常见注意事项。

文档按 `src/` 下的模块目录建立对应关系，保持目录名称及大小写一致，不要求逐个源码文件建立文档。例如 `src/core/runtime/` 对应 `docs/design/core/runtime.md` 与 `docs/usage/core/runtime.md`，`src/modules/roomShortcuts/` 对应两类目录下的 `modules/roomShortcuts.md`。跨模块架构设计放在所属层级的 `README.md`，例如 `docs/design/core/README.md`。尚未交付的模块可以按设计确定的目标源码路径建立设计文档，必须标注交付状态，不得编造使用说明或链接到不存在的源码。

新增模块时必须同时创建两类文档。修改模块公共行为、配置或返回契约时，必须同步更新对应设计文档和使用说明。

今后新增或修改的所有设计案必须遵守上述内容边界。严格禁止在设计案中出现项目现状对照相关内容，包括现有实现盘点、当前与目标的对比表、重构前后差异、既有代码与设计的差距分析，以及用“当前如何、未来如何”组织的对照叙述。交付状态只能标明设计项是否交付，不得借交付状态展开项目现状对照。设计所需的迁移协议、兼容约束和恢复机制可以作为设计意图描述，但不得写成现状对照。现有公共能力的使用方法写入使用说明，已完成变更的历史记录写入 `docs/changelog/`。

仓库根 `README.md` 链接 `docs/README.md` 作为文档总导航；总导航按模块列出源码、设计与使用说明，缺失项明确标注。`docs/design/README.md` 和 `docs/usage/README.md` 分别作为两类文档索引。模块文档新增、移动或更名时，同步更新导航、索引及所有引用（包括源码注释中的路径）。已经完成的阶段性更新记录放在 `docs/changelog/`，按日期组织，并同步更新该目录索引。

## 7. 构建与验证

完成代码或构建系统修改后，应依次完成与变更相符的验证，通常包括：

```bash
npx tsc --noEmit
npm test
npm run build
git diff --check
```

`npm run build` 必须在没有 `.secret.json` 时也能成功。验证环境上传统一使用 `npm run upload:validation`；只有用户明确要求部署时才执行外部上传。

## 8. 密钥与敏感信息

严禁提交 `.secret.json`、认证令牌、账号凭据或包含真实凭据的命令输出。公开配置示例不得包含真实密钥。

## 9. Memory 访问边界

`src/core/memoryManager` 是项目访问游戏持久化存储的唯一入口。除该模块自身及其平台适配层外，任何源码文件都严禁直接访问或改写持久化存储，包括但不限于：

- 全局 `Memory` 对象：读取、赋值、删除字段，或用 `globalThis.Memory` 挂载替身；
- `RawMemory`：`get`/`set`/`segments` 及 Segment 激活接口；
- 其它持久化旁路：自行解析存储文本、把跨 global 状态塞进外部共享对象等。

需要跨 global 保留状态的模块，必须通过 `context.memory(localId, options)` 申请分区，在 `pending` 时只跳过依赖存储的行为，其余逻辑照常执行。审查与自动化检查中发现的越界访问按阻断问题处理，而不是风格问题；`test/memoryBoundary.test.ts` 会扫描 `src/` 并在越界时报错。

## 10. Core 同级依赖边界

`src/core/` 下的同级能力模块不得直接导入彼此的具体实现。模块之间只允许依赖 `src/contracts/` 发布的协议，并由调用方显式注入所需实例；严禁在模块内部通过默认参数、模块级单例或临时工厂创建同级依赖的兜底实现。

`src/core/runtime` 是唯一的 Core 组合根。它可以导入 Logger、EventBus、MemoryManager、Profiler、ErrorMapper 与 TaskScheduler 的具体工厂，按依赖有向无环图从前序到后序创建实例，并将完整 Runtime 交给 Framework。前序模块必须严格不依赖后序模块；Framework 只消费 Runtime 契约，不得再创建或替换其中的 Core 能力。

独立单元测试也必须在测试装配边界显式提供依赖，不能以恢复生产代码中的隐藏缺省依赖换取测试便利。`test/coreDependencyBoundary.test.ts` 会扫描 Core 源码中的跨模块导入并在越界时报错。
